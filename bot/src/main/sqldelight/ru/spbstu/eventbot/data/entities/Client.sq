CREATE TABLE Client (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email TEXT NOT NULL,
    name TEXT NOT NULL
);

insert:
INSERT INTO Client(name, email)
VALUES (:name, :email);

findByChatId:
SELECT * FROM Client
WHERE chatId = :chatId;
