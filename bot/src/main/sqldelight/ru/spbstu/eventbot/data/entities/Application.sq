CREATE TABLE Application (
id INTEGER PRIMARY KEY AUTOINCREMENT,
userId INTEGER NOT NULL,
courseId INTEGER NOT NULL,
FOREIGN KEY (userId) REFERENCES User (id),
FOREIGN KEY (courseId) REFERENCES Course (id)
);

insert:
INSERT INTO Application(userId, courseId)
VALUES (?, ?);

-- количество
getCount:
SELECT Count(0)
FROM Application
WHERE courseId = ?;


-- id студента сами как то разберитесь =)
getInfoAboutStudents:
SELECT userId
FROM  Application
WHERE  courseId =?;
