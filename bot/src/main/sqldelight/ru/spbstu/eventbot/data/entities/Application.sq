CREATE TABLE Application (
id INTEGER PRIMARY KEY AUTOINCREMENT,
user_id INTEGER NOT NULL,
course_id INTEGER NOT NULL,
FOREIGN KEY (user_id) REFERENCES User (id),
FOREIGN KEY (course_id) REFERENCES Course (id)
);

insert:
INSERT INTO Application(user_id, course_id)
VALUES (?, ?);

-- количество
getCount:
SELECT Count(0)
FROM Application
WHERE courseId = ?;


-- id студента сами как то разберитесь =)
getInfoAboutStudents:
SELECT user_id
FROM  Application
WHERE  course_id = ?;
