CREATE TABLE User (
id INTEGER PRIMARY KEY AUTOINCREMENT,
chatId INTEGER NOT NULL,
email TEXT NOT NULL,
name TEXT NOT NULL,
groupNumber TEXT NOT NULL
);

selectAll:
SELECT *
FROM User;

findByChatId:
SELECT *
FROM User
WHERE chatId = ?;